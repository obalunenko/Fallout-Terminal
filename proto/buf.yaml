version: v2

modules:
  - path: .

lint:
  use:
    - STANDARD
  except:
    # The public contract intentionally uses one stable ActionResult for every
    # mutation and one SubscriptionMessage envelope for the server stream.
    - RPC_REQUEST_RESPONSE_UNIQUE
    - RPC_RESPONSE_STANDARD_NAME

breaking:
  use:
    - FILE
